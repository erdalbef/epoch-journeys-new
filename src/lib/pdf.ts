export async function generateQuotePdfUrl(quoteId: string) {
  // Replace this later with real PDF generation logic.
  // Example final output could be:
  // /api/quotes/${quoteId}/pdf/download
  return `/admin/quotes/${quoteId}/pdf`;
}