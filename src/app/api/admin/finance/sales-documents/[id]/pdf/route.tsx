import React from "react";import { NextResponse } from "next/server";import { getServerSession } from "next-auth";import { renderToBuffer } from "@react-pdf/renderer";import { Role } from "@prisma/client";import { authOptions } from "@/lib/authOptions";import { buildSalesPdfData } from "@/lib/sales-document-pdf-data";import { SalesDocumentPdf } from "@/lib/pdf/SalesDocumentPdf";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const session=await getServerSession(authOptions);if(!session?.user||session.user.role!==Role.ADMIN)return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params;const data=await buildSalesPdfData(id);if(!data)return NextResponse.json({error:"Issue the document before generating its PDF."},{status:400});if(!data.fontRegular||!data.fontBold)return NextResponse.json({error:"Bulgarian PDF font files are missing. Add NotoSans-Regular.ttf and NotoSans-Bold.ttf under public/fonts."},{status:500});const buffer=await renderToBuffer(<SalesDocumentPdf data={data}/>);return new Response(new Uint8Array(buffer),{headers:{"Content-Type":"application/pdf","Content-Disposition":`inline; filename="${data.documentNumber}.pdf"`}})}
