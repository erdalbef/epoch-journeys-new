"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function DocumentActions({id,status,email}:{id:string;status:string;email:string|null}){const router=useRouter();const[busy,setBusy]=useState("");const[error,setError]=useState("");async function act(action:string){setBusy(action);setError("");const r=await fetch(`/api/admin/finance/sales-documents/${id}/${action}`,{method:"POST"});const d=await r.json().catch(()=>null);setBusy("");if(!r.ok){setError(d?.error||"Action failed.");return}router.refresh();}return <div className="space-y-2"><div className="flex flex-wrap gap-2">{status==="DRAFT"&&<button onClick={()=>act("issue")} disabled={!!busy} className={primary}>Issue Document</button>} {status!=="DRAFT"&&status!=="CANCELLED"&&<a href={`/api/admin/finance/sales-documents/${id}/pdf`} target="_blank" className={secondary}>Open PDF</a>} {status!=="DRAFT"&&status!=="CANCELLED"&&email&&<button onClick={()=>act("send")} disabled={!!busy} className={secondary}>{busy==="send"?"Sending...":"Send by Email"}</button>}</div>{error&&<p className="text-sm text-red-700">{error}</p>}</div>}
const primary="rounded-xl bg-[#8B0000] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50";const secondary="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 hover:border-[#8B0000] hover:text-[#8B0000] disabled:opacity-50";
